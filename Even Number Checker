function isEven(n, callback) {
  function check(num) {
    if (num === 0) return true;
    if (num === 1) return false;
    return check(num - 2);
  }

  const result = check(n);
  callback(result);
}
